-extends "corporate/private_base.haml"

-set title = "Taxis"

-import "form_macros.haml" as form_macros

-block content:
  .container
    %p.lead
      %a(href="#{uri_for('taxi.list')}") Taxi
      &raquo;
      -if 'update' in request.url
        Update Taxi
      -elif 'set_driver' in request.url
        Set driver
      -elif 'create' in request.url
        Add Taxi

    %form.form-horizontal(action=".", method="POST")
      #{form_macros.form_fields(form)}
      #{form_macros.submit_button()}
